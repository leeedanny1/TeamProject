공지사항 페이지 불러와보기 해 보고 싶다.

repository랑 매퍼, 서비스 작성 했다.

컨트롤러에서 서비스를 호출하면 실행이 된다.

그래서 호출하면 -> db연결 -> 매퍼 -> 쿼리 날리고 결과 -> 서비스 -> dto로 들어감.

--------------------------------------------------

select 결과를 el??그걸로 불러와서 jsp에 적어주면 결과가 나온다.

그 el등록하는걸 어떻게 해서 어디서 불러와야 되느냐??


----------------------------------------------------
notice controller에서 작성자와 세션닉네임 동일한경우에만 가능하게